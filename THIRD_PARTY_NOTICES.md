# Third-party notices

The initial reader uses Rust standard-library parsing. Workspace dependencies, when added, remain
identified in `Cargo.lock` and are used only for bounded service or contract handling. No
third-party provider SDK or proprietary game code is included.
