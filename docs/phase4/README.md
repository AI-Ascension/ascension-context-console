# Phase 4 fixture lane

The target's provider-session surface is opt-in and additive. Run `cargo run --locked -p
context-service -- phase4-cli capabilities` or `phase4-cli candidate` for the bounded offline
projection. These commands use no credentials, provider network, game process, or persistent
native session. The harness owns the real adapter boundary and must keep explicit resume as the
only execution release.
